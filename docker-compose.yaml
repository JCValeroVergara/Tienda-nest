version: '1'

services:
  db:
    image: postgres:14.3
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres@123
      POSTGRES_DB: TesloDB
    container_name: teslodb
    volumes:
      - ./postgres:/var/lib/postgresql/data
